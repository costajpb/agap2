import useLinkToActionHandler from "."
import UseCase from "../../application/shared/use-case"
import Entity from "../../domain/shared/entity"

describe('hooks', () => {
    it('should prevent default', () => {
       const abc = true
    })
})